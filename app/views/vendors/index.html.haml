%header
%div.centeredimagezindex5
  %div.headergraphic
%div.headerblue
  = link_to "Logout", destroy_user_session_path, method: :delete, :class => 'navbar-link'
-if current_user
  Logged in as 
  = current_user.email
%div.centeredtruckentries
  = render partial: "openVendor", collection: @openVendors
  = render partial: "test_vendor", collection: @vendors
  = link_to 'Log out', destroy_user_session_path, method: :delete

%button.btn.btn-default.create-button= link_to "+ New Vendor", new_vendor_path